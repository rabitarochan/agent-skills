---
description: Design a feature addition to an existing project through dialogue, then write the design document
argument-hint: "[feature summary, issue number, ... (optional)]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/design-feature/SKILL.md` and follow it for this
session. Read the reference files it points to, and investigate the existing
codebase as the skill instructs, before asking the user anything.

Context from the user: $ARGUMENTS
